package br.com.A3.controllers;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping(value = "/produtos")
public class ProdutoController {

	/*
	 * @GetMapping public List<Produto> findAll() { return repository.findAll(); }
	 * 
	 * @GetMapping(value = "/{id}") public Produto findAll(@PathVariable Long id) {
	 * return repository.findById(id).get(); }
	 */

	/*
	 * @PostMapping public Produto insert(@RequestBody Produto produto) { return
	 * repository.save(produto); }
	 */

}
